import { SiteConfig } from "@/interfaces/site";

export const siteConfig: SiteConfig = {
  name: "Local Scaling",
  description: "Description of Local Scaling",
  url: "https://local.vercel.app",
  keywords: ["local", "Scaling"],
};
